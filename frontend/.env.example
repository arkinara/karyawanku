# KaryawanKu FE — environment (copy to .env.local)
#
# Base URL of the Fastify backend. The API client reads this at runtime; any
# call without NEXT_PUBLIC_API_BASE_URL falls back to the dev default.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001
