<?xml version="1.0" encoding="utf-8"?>
<!-- Ticket #74 — widget card surface: rounded rect, ProMax surface fill +
     hairline, matching the in-app card look. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/kk_surface" />
    <corners android:radius="16dp" />
    <stroke
        android:width="1dp"
        android:color="@color/kk_outline_variant" />
</shape>